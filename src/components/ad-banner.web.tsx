/**
 * Web stub for AdBanner.
 *
 * `react-native-google-mobile-ads` is a native-only module; this stub keeps the
 * web bundle free of it. No space is reserved on web either — see
 * `adBannerHeight` in `@/constants/theme` (0 on web).
 */
export function AdBanner() {
  return null;
}
